# MongoSQL - SQL to MongoDB Translation Engine
# Main requirements file for complete development and testing environment

# Core Runtime Dependencies
pymongo>=4.6.0             # MongoDB driver for database connections
python-dotenv>=1.0.0       # Environment variable loading from .env files
click>=8.1.0                # Command-line interface framework
sqlparse>=0.4.4             # SQL parsing and tokenization library

# QA Testing Dependencies
mysql-connector-python>=8.2.0  # MariaDB/MySQL connector for QA comparison testing

# User Experience Dependencies
colorama>=0.4.6             # Cross-platform colored terminal text
rich>=13.7.0                # Rich text and beautiful formatting in terminal

# Development Dependencies (Optional but recommended)
black>=23.0.0               # Python code formatter
pylint>=3.0.0               # Python code linter and quality checker

# Installation Instructions:
# Production only:     pip install -r requirements-prod.txt
# Full development:    pip install -r requirements.txt
# Development extras:  pip install -r requirements-dev.txt
